const RECOVERY_SCHEMA_VERSION =
  1;


const RECOVERY_DIR_NAME =
  ".pl-recovery";


const AUTOSAVE_DIR_NAME =
  "autosaves";


const STATUS_FILE_NAME =
  "status.json";


const JOURNAL_FILE_NAME =
  "journal.jsonl";


const SNAPSHOT_DIR_NAME =
  "snapshots";


const DEFAULT_MAX_SNAPSHOTS =
  20;

module.exports = {
  RECOVERY_SCHEMA_VERSION,
  RECOVERY_DIR_NAME,
  AUTOSAVE_DIR_NAME,
  STATUS_FILE_NAME,
  JOURNAL_FILE_NAME,
  SNAPSHOT_DIR_NAME,
  DEFAULT_MAX_SNAPSHOTS,
};
